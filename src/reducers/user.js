const initialState = {
    displayName: "Life. by Tyler",
    bio: "I am sort of the coolest dude ever while at the same time being such a complete and utter nerd that it is incredibly hillarious for most poeple.",
    username: "lifebytyler"
}

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}